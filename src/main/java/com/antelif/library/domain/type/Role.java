package com.antelif.library.domain.type;

/** Personnel role in the system. */
public enum Role {
  ADMIN;

  /** Returns the string value of the enum. */
  public String getName() {
    return super.name();
  }
}
